# 4-bit-ALU
A modular 4-bit arithmetic logic unit (ALU) designed and simulated in SystemVerilog. Supports arithmetic and bitwise operations with status flags, serving as the foundation for a custom 8-bit CPU.
